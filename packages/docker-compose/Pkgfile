name=docker-compose
version=1.1.0
release=1
source=(https://github.com/docker/compose/releases/download/$version/$name-$(uname -s)-$(uname -m))

build() {
  install -D -m 755 $name-$(uname -s)-$(uname -m) $PKG/usr/bin/$name
}
